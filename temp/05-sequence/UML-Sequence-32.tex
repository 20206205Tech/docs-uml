\paragraph{Sơ đồ tuần tự Pha Transactional Outbox đồng bộ trạng thái VIP}

Pha này mô tả Outbox Relay Cron của Payment Service quét sự kiện đang chờ xử lý, publish sự kiện \texttt{SubscriptionPurchased} lên Kafka và Conversation Service tiêu thụ sự kiện để cập nhật trạng thái VIP cục bộ.

\begin{figure}[H]
\centering
\includegraphics[scale = 0.18]{pictures/UML-Sequence-32.png}
\caption{Sơ đồ tuần tự Pha Transactional Outbox đồng bộ trạng thái VIP}
\end{figure}

\subparagraph{Các thành phần tham gia}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|l|l|p{5cm}|}
\hline
\textbf{Thành phần} & \textbf{Phân loại} & \textbf{Vai trò trong hệ thống} \\ \hline
Payment Service & Microservice & Chạy Outbox Relay Cron, publish event và đánh dấu sự kiện đã xử lý. \\ \hline
Payment DB & Database & Lưu bảng Outbox và trạng thái xử lý sự kiện. \\ \hline
Kafka & Message Broker & Event bus truyền tải sự kiện mua gói VIP. \\ \hline
Conversation Service & Microservice & Tiêu thụ sự kiện để đồng bộ trạng thái subscription trong local DB. \\ \hline
\end{tabular}
\caption{Các thành phần tham gia pha Transactional Outbox đồng bộ trạng thái VIP}
\end{table}

\subparagraph{Mô tả chi tiết}
\begin{itemize}
\item \textbf{Quét Outbox:} Outbox Relay Cron định kỳ đọc các sự kiện \texttt{PENDING}.
\item \textbf{Publish sự kiện:} Payment Service publish \texttt{SubscriptionPurchased} lên Kafka và cập nhật Outbox thành \texttt{DONE}.
\item \textbf{Đồng bộ consumer:} Conversation Service tiêu thụ event và cập nhật UserSubscription local thành \texttt{ACTIVE}.
\end{itemize}
